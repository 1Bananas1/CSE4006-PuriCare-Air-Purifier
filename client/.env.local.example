# Firebase Web App Configuration
# Get these values from Firebase Console > Project Settings > General > Your apps > Web app
# NOTE: These match your existing Vercel environment variable names

# Firebase Web API Key (from Firebase Console)
NEXT_PUBLIC_FIREBASE_WEB_API_KEY=your-api-key-here

# Firebase Project ID (should already be set in Vercel)
NEXT_PUBLIC_FIREBASE_PROJECT_ID=cse4006

# Firebase Messaging Sender ID (should already be set in Vercel)
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-sender-id

# Firebase App ID (from Firebase Console > Your apps > Web app)
NEXT_PUBLIC_FIREBASE_APP_ID=your-app-id

# VAPID Key for Web Push
# Get this from Firebase Console > Project Settings > Cloud Messaging > Web Push certificates
# Click "Generate key pair" if you don't have one
NEXT_PUBLIC_FIREBASE_VAPID_KEY=your-vapid-key-here

# API URL (your backend server - should already be set in Vercel)
NEXT_PUBLIC_API_URL=https://your-app.herokuapp.com
